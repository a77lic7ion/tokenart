# Generated at runtime by collect-usage.py — personal telemetry, not source.
# Deliberately NOT committed: these hold this machine's session counts, token totals
# and per-account spend. The page degrades gracefully without them (it says so on screen).
usage.json
history.json

# Editor and OS noise
*.swp
*~
.DS_Store

# Scratch folder the deck builder serves from. Build artifacts, never source.
_deck/